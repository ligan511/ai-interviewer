# AI 智能面试官与求职能力评估系统 — 启动指南

## 一、环境要求

| 依赖 | 版本 | 说明 |
|---|---|---|
| Java | 17+ | 后端 Spring Boot 3.2.5 |
| Maven | 3.9+ | 编译后端 |
| Node.js | 18+ | 前端 Vue 3 + Vite 5 |
| Python | 3.9+ | AI 服务 FastAPI |
| MySQL | 8.0 | 数据库 |
| Redis | 6+ | 预留（当前未强制使用） |

## 二、项目结构

```
├── src/
│   ├── backend/          # Java Spring Boot 主业务服务 (端口 8081)
│   │   ├── pom.xml
│   │   └── src/main/java/com/aiinterviewer/
│   │       ├── AiInterviewerApplication.java
│   │       ├── common/     # ApiResponse, SecurityContext, GlobalExceptionHandler
│   │       ├── config/     # MybatisPlus, AutoFill, RestTemplate, AsyncConfig, WebMvcConfig
│   │       ├── controller/ # Auth, Job, Interview, Question, Answer, Report, Dashboard
│   │       ├── entity/     # 12 张实体类
│   │       ├── mapper/     # 11 个 MyBatis-Plus Mapper
│   │       ├── security/   # JWT 认证 + InternalKeyFilter
│   │       └── service/    # 业务接口 + 实现
│   ├── frontend/         # Vue 3 + TypeScript + Element Plus (端口 5173)
│   │   ├── package.json
│   │   └── src/
│   │       ├── api/       # axios 封装 + auth/job/interview/knowledge/resume API
│   │       ├── layouts/   # MainLayout
│   │       ├── router/    # 路由 + 鉴权守卫
│   │       ├── stores/    # Pinia user store
│   │       └── views/     # Login/Register/Dashboard/Jobs/Interview/Report/History/Profile/Knowledge
│   └── ai-service/       # Python FastAPI AI 服务 (端口 8001)
│       ├── main.py
│       ├── requirements.txt
│       └── .env.example  # 复制为 .env 并填入真实 API key
├── docs/
├── docker-compose.yml    # 一键 Docker 部署
├── .github/workflows/    # CI（mvn test + pytest + vite build）
└── AI智能面试官系统_MySQL_V1.0.sql
```

## 三、首次交接 / 新环境启动

### 第 1 步：克隆仓库

```bash
git clone https://github.com/ligan511/ai-interviewer.git
cd ai-interviewer
```

### 第 2 步：配置数据库

执行建库脚本（MySQL 8）：

```bash
mysql -u root -p < AI智能面试官系统_MySQL_V1.0.sql
# 验证
mysql -u root -p -e "USE ai_interviewer; SHOW TABLES;"
```

记下你的 MySQL root 密码，后续设为 `MYSQL_PASSWORD` 环境变量。

### 第 3 步：配置 AI 服务密钥

```bash
cd src/ai-service
cp .env.example .env
# 编辑 .env，填入：
#   AGNES_API_KEY=cpk-xxx        # Agnes AI 出题/评分/报告
#   SILICONFLOW_API_KEY=sk-xxx  # SiliconFlow RAG embedding（可选）
#   AI_INTERNAL_KEY=一个强随机字符串  # 后端调用内部接口的鉴权 key
```

> `.env` 已在 `.gitignore` 中，不会进版本库。仓库只含 `.env.example` 模板。

### 第 4 步：准备后端环境变量

`application.yml` 中 `MYSQL_PASSWORD`、`AI_INTERNAL_KEY`、`JWT_SECRET` 均为 `:?` 强制注入，**必须**设置：

```bash
# Windows PowerShell
$env:MYSQL_PASSWORD = "你的MySQL密码"
$env:AI_INTERNAL_KEY = "与 .env 中 AI_INTERNAL_KEY 一致的值"
$env:JWT_SECRET = "至少 32 字符的强随机字符串（HMAC-SHA 要求 256 位以上）"
```

```bash
# Linux / macOS
export MYSQL_PASSWORD="你的MySQL密码"
export AI_INTERNAL_KEY="与 .env 中一致"
export JWT_SECRET="至少32字符"
```

> `JWT_SECRET` 过短会导致所有 JWT 校验失败（后端日志报 "key byte array is N bits not secure enough"）。

### 第 5 步：启动 AI 服务（8001）

```bash
cd src/ai-service
pip install -r requirements.txt
python main.py
# 验证: curl http://localhost:8001/health
```

### 第 6 步：启动后端（8081）

```bash
cd src/backend
mvn spring-boot:run
# 验证: curl http://localhost:8081/api/v1/auth/health
```

### 第 7 步：启动前端（5173）

```bash
cd src/frontend
npm install
npm run dev
# 浏览器访问 http://localhost:5173
```

### 第 8 步（可选）：Docker 一键部署

```bash
# 设置必需环境变量后
export MYSQL_PASSWORD="..." AI_INTERNAL_KEY="..." JWT_SECRET="..."
docker compose up -d --build
```

## 四、本地快速启动脚本（Windows）

仓库根目录提供：
- `start-all.ps1` — 一次性启动 AI 服务 + 后端 + 前端
- `start-backend.bat` / `start-frontend.bat` / `start-ai-service.bat` — 分别启动

使用 `start-all.ps1` 前需先在 PowerShell 中设好 3 个环境变量（第 4 步）。

## 五、核心功能清单（V1.0）

| # | 功能 | 状态 |
|---|---|---|
| 1 | 用户注册/登录（JWT） | ✅ |
| 2 | 岗位浏览 | ✅ |
| 3 | 面试配置（类型/难度/题数） | ✅ |
| 4 | AI 出题（Agnes LLM + mock 兜底） | ✅ |
| 5 | 文字/语音回答提交 | ✅ |
| 6 | AI 六维评分（专业能力/逻辑/完整度/分析/表达/岗位匹配） | ✅ |
| 7 | AI 动态追问 | ✅ |
| 8 | 面试状态机（CREATED→IN_PROGRESS→COMPLETED） | ✅ |
| 9 | 面试报告（雷达图 + 优劣势 + 建议） | ✅ |
| 10 | 历史面试 + 能力趋势 | ✅ |
| 11 | 知识库 RAG 检索 | ✅（P1） |
| 12 | 语音转文字（Web Speech API，Chrome/Edge） | ✅（P1） |

## 六、关键约定与坑

1. **`JWT_SECRET` 必须 ≥ 32 字符**，否则所有 JWT 校验失败（jjwt 要求 256 位以上）。
2. **`AI_INTERNAL_KEY` 后端和 AI 服务必须一致**，否则内部接口 401。
3. **`application.yml` 强制 `:?` 注入**，不设 3 个环境变量后端无法启动。
4. **语音识别仅 Chrome/Edge** 支持 Web Speech API，其他浏览器回退为"仅存音频"。
5. **每题限时 300s**（前端控制），超时自动记空答并跳下一题。
6. **文件上传**用 `MultipartFile.transferTo(Path)`，路径必须**绝对路径**（`WebMvcConfig` 映射 `/uploads/**`）。
7. **MyBatis-Plus `JacksonTypeHandler`** 需要实体加 `@TableName(autoResultMap = true)`，否则查询时 List/Map 字段为 null。

## 七、后续开发建议

1. **ASR 语音转文字**：当前仅存音频 + Web Speech 前端识别，后端未接 DashScope ASR；可接入 `DASHSCOPE_API_KEY` 提升精度。
2. **Redis**：V1.0 预留未使用，P1 接入会话锁和缓存。
3. **RAG 知识库**：当前 SQLite + numpy 向量库，P1 可升级 pgvector / Milvus。
4. **前端测试**：当前无 Vitest，建议补 e2e 覆盖面试流程。
5. **后端集成测试**：当前仅单元测试，建议补 controller 层 `@SpringBootTest`。
