# AI 智能面试官与求职能力评估系统 — 启动指南

## 一、环境要求

- **Java 11+**（已安装 JDK 11.0.17）
- **Node.js 18+**（已安装 v24.14.1）
- **Python 3.9+**（已安装 3.13.9）
- **MySQL 8.0**（已安装，需配置密码）
- **Maven 3.9+**（已安装）

## 二、项目结构

```
D:\AI 智能面试官与求职能力评估系统\
├── src/
│   ├── backend/          # Java Spring Boot 主业务服务 (端口 8080)
│   │   ├── pom.xml
│   │   └── src/main/java/com/aiinterviewer/
│   │       ├── AiInterviewerApplication.java
│   │       ├── common/     # ApiResponse, SecurityContext
│   │       ├── config/     # MybatisPlus, AutoFill, RestTemplate
│   │       ├── controller/ # Auth, Job, Interview, Question, Answer, Report
│   │       ├── entity/     # 12 张实体类
│   │       ├── mapper/     # 11 个 MyBatis-Plus Mapper
│   │       ├── security/   # JWT 认证
│   │       ├── service/    # 业务接口
│   │       └── service/impl/
│   ├── frontend/         # Vue 3 + TypeScript + Element Plus (端口 5173)
│   │   ├── package.json
│   │   └── src/
│   │       ├── api/      # axios 封装 + auth/job/interview API
│   │       ├── components/
│   │       ├── layouts/  # MainLayout
│   │       ├── router/   # 路由配置 + 鉴权守卫
│   │       ├── stores/   # Pinia user store
│   │       └── views/    # Login/Register/Dashboard/Jobs/Interview/Report/History/Profile
│   └── ai-service/       # Python FastAPI AI 服务 (端口 8001)
│       ├── main.py
│       └── requirements.txt
├── docs/
└── AI智能面试官系统_MySQL_V1.0.sql
```

## 三、启动步骤

### 1. 配置 MySQL

```bash
# 连接 MySQL（需要替换密码）
mysql -u root -p

# 执行建库脚本
source "D:\AI 智能面试官与求职能力评估系统\AI智能面试官系统_MySQL_V1.0.sql"

# 验证
SHOW DATABASES;
USE ai_interviewer;
SHOW TABLES;
```

### 2. 启动 AI 服务（Python FastAPI）

```bash
cd "D:\AI 智能面试官与求职能力评估系统\src\ai-service"
pip install -r requirements.txt
python main.py
# 访问 http://localhost:8001/docs 查看 OpenAPI 文档
```

### 3. 启动后端（Spring Boot）

编辑 `src/backend/src/main/resources/application.yml`，设置 MySQL 密码：

```yaml
spring:
  datasource:
    password: 你的MySQL密码
```

```bash
cd "D:\AI 智能面试官与求职能力评估系统\src\backend"
mvn compile
mvn spring-boot:run
# 访问 http://localhost:8080/api/v1/auth/login 测试
```

### 4. 启动前端（Vue 3）

```bash
cd "D:\AI 智能面试官与求职能力评估系统\src\frontend"
npm run dev
# 访问 http://localhost:5173
```

## 四、V1.0 核心功能清单

| # | 功能 | 状态 |
|---|------|------|
| 1 | 用户注册/登录（JWT 认证） | ✅ 完成 |
| 2 | 岗位浏览与选择 | ✅ 完成 |
| 3 | 面试配置（类型/难度/题数/时长） | ✅ 完成 |
| 4 | AI 自动生成面试题 | ✅ 完成（Mock） |
| 5 | 用户文字回答提交 | ✅ 完成 |
| 6 | AI 多维度评分（6 维度） | ✅ 完成（Mock） |
| 7 | AI 动态追问 | ✅ 完成（Mock） |
| 8 | 面试流程控制（状态机） | ✅ 完成 |
| 9 | AI 面试报告生成 | ✅ 完成（Mock） |
| 10 | 历史面试记录 | ✅ 完成 |

## 五、后端编译验证

```bash
cd src/backend && mvn compile
# 输出: BUILD SUCCESS，52 个源文件编译通过
```

## 六、前端构建验证

```bash
cd src/frontend && npm run build
# 输出: ✓ built in 53.76s，dist/ 目录生成完毕
```

## 七、后续开发建议

1. **接入真实 LLM**：修改 `ai-service/main.py`，将 Mock 数据替换为真实 LLM API 调用
2. **MySQL 密码**：从环境变量读取，不要硬编码
3. **Redis**：V1.0 暂未使用，P1 阶段接入会话锁和缓存
4. **RAG 知识库**：P1 阶段接入 pgvector/Milvus
5. **语音功能**：P1 阶段接入 ASR，P2 阶段接入 TTS
